{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd webhook-server && npm install"
  },
  "deploy": {
    "startCommand": "cd webhook-server && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
