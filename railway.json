{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd webhook-server && npm ci"
  },
  "deploy": {
    "startCommand": "cd webhook-server && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
